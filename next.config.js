/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    assetPrefix: 'https://ali-jabbari.ir/'
    // images: {
    //     unoptimized: true
    // }
}

module.exports = nextConfig
